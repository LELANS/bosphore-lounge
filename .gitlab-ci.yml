pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/index.html
    - cp -r assets public/assets
  artifacts:
    paths:
      - public
  pages: true
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
